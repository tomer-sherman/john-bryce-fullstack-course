export type EmployeeModel = {
    id: number;
    firstName: string;
    lastName: string;
    title: string;
    country: string;
    city: string;
    birthDate: string; // ISO format string (e.g., "1948-12-08")
    imageUrl: string;
};