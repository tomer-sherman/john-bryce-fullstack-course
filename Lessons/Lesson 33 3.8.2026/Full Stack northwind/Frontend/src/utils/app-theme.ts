import { createTheme } from "@mui/material";

export const appTheme = createTheme({
    typography: {
        fontFamily: "cursive"
    },
    palette: {
        primary: {
            main: "#e17c08"
        },
        secondary: {
            main: "#2b741c"
        }
    }
});

