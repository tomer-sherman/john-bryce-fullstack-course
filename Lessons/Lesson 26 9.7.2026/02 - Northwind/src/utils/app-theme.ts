import { createTheme } from "@mui/material";

export const appTheme = createTheme({

    typography: {
        fontFamily: "cursive"
    },

    palette: {
        primary: {
            main: "#c50f0f"
        },
        secondary: {
            main: "#12d342"
        }
    }


});
