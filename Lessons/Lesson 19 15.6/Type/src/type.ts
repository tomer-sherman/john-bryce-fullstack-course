export type PrintingOptions = { 
    header?: string, 
    delimiter?: string, 
    newLine?: boolean 
}

// Using the ? operator, You don't have too give all the options, You have too give just delimiter in this case.
