// קילטו משמשתמש שני מסספרים הציגו את המקסימלי אם שניהם שווים יש להציג אחד מהם.

//const num1 = +prompt("Enter a number :"); 
//const num2 = +prompt("Enter a number :");
//const max = (num1 > num2 ? num1 : num2);
//document.write("max:"+max);

const grade = prompt("Enter a number:");

const smiley= grade >= 60 && grade <= 100 ? "😊" : "😒";

document.write(smiley);

// Operator Modulo written like this %
// an operator that give a part of 2 variables.